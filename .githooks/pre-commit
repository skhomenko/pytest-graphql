#!/bin/sh
# pytest-graphql pre-commit hook. Thin dispatcher.
# The checks live in scripts/git_hook_checks.py, which is self-tested.
set -eu
exec python3 "$(git rev-parse --show-toplevel)/scripts/git_hook_checks.py" pre-commit
