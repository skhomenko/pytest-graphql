"""Schema-aware GraphQL test client with an optional pytest plugin.

The public API is re-exported from this module. Nothing is exported yet: the
library modules land in later milestones. ``__version__`` is the single source
of the distribution version, and the release workflow checks it against the tag.
"""

from __future__ import annotations

__all__ = ["__version__"]

__version__ = "0.1.0a1.dev0"
