"""The hostile test schema: SDL, resolvers, and an in-process fake transport.

This package is fixture data shared across test layers (SPEC 11.1, 11.2). It
imports neither pytest nor anything under ``src/``.
"""
